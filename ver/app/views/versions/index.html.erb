<%= form_tag versions_index_path, :method => :get do %>
      <%= text_field_tag :search, params[:search] %>
      <%= submit_tag "Search", :name => nil, :class => "button postfix" %>
<% end %>

<% if @products.empty? %> 
    <p>No products added to database</p>
<% else %>
    <ul>
      <% @products.each do |product| %>
          <li>
         Version:   <%= product.versionnumber %><br> 
         Eventt: <%= link_to product.incident, showw_path(product) %><br>
         Modifications:   <%= product.modifications %>
          </li>
          <hr>
      <% end %>
    </ul>
<% end %>
hey
<% for row in @query.facet(:incident).rows %>
hey2
      <li>
        <% if params[:inci].blank? %>
          <%= link_to row.value, :inci=> row.value %> (<%= row.count %>)
        <% else %>
          <strong><%= row.value %></strong> (<%= link_to "remove", :inci => nil %>)
        <% end %>
      </li>
    <% end %>
hey3


<%= link_to 'New', versions_start_path %>
